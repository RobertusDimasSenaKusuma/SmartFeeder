<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="45"
        android:startColor="@color/dynamic_button_active_background"
        android:endColor="@color/dynamic_button_active_background"/>
    <corners android:radius="12dp" />
    <!-- Opsional: Tambahkan stroke untuk menandakan state aktif lebih jelas -->
    <!-- <stroke android:width="2dp" android:color="@color/white"/> -->
</shape>